{/* 
imports

describe('Server Routes', () => {

test('GET /api/v1/ai returns AI data', async () => {
(aiService.getAIResponse as jest.Mock).mockResolvedValue({text: 'Hello AI'}

const res
expect(res.status)
expect(res.body.text)
})

api missing input returns 400

see also
movie routes
success
review success
empty results
missing query
server error handling

})

  */}